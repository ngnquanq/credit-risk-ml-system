# postgres-data.yaml - Please use this EXACT content
source:
  type: postgres
  config:
    host_port: "localhost:5432"
    username: "quang_user"
    password: "quang2011"
    database: "homecredit"
    schema: "data"

sink:
  type: datahub-rest
  config:
    # --- START OF NEW CORRECTION ---
    server: # <--- ADD THIS LINE, ensuring correct indentation
      gms_host: "localhost"
      gms_port: 8080
    # --- END OF NEW CORRECTION ---
    # The 'auth_token' would go here if you were using it, at the same level as 'server'
    # auth_token: "YOUR_DATAHUB_API_TOKEN"

pipeline:
  name: "homecredit_postgres_ingestion"
  processors:
    - type: add_dataset_ownership
      config:
        owner_urn: "urn:li:corpuser:datahub"

    - type: add_dataset_tags
      config:
        tags:
          - "homecredit"
          - "kaggle-competition"