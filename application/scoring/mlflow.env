SCORING_MLFLOW_MODEL_URI=models:/credit_risk_model/Production
SCORING_MODEL_NAME=credit_risk_model
SCORING_MODEL_VERSION=12
SCORING_ENABLE_KAFKA=true
SCORING_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
SCORING_LOAN_APPLICATION_TOPIC=hc.applications.public.loan_applications
SCORING_SCORING_OUTPUT_TOPIC=hc.scoring
# SCORING_FEAST_REPO_PATH will use absolute path calculated from config.py
# Uncomment and modify this line only if you need to override the default:
# SCORING_FEAST_REPO_PATH=/path/to/your/feast/repo

# Inline Feast config (use these if you don't mount the repo)
# SCORING_FEAST_REGISTRY_URI=s3://my-bucket/feast/registry.db
# SCORING_FEAST_REDIS_URL=redis://feast_redis:6379/0
# SCORING_FEAST_PROJECT=hc
# SCORING_FEAST_PROVIDER=local

# MLflow connection settings
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_S3_ENDPOINT_URL=http://localhost:9006
AWS_ACCESS_KEY_ID=minio_user
AWS_SECRET_ACCESS_KEY=changeme123
