apiVersion: eventing.knative.dev/v1alpha1
kind: KafkaSink
metadata:
  name: scoring-dlq-sink
  namespace: kserve
spec:
  bootstrapServers:
    - kafka-broker.data-services.svc.cluster.local:9092

  topic: hc.scoring.dlq

  contentMode: binary
