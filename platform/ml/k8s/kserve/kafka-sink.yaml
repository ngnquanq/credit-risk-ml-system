apiVersion: eventing.knative.dev/v1alpha1
kind: KafkaSink
metadata:
  name: scoring-output-sink
  namespace: kserve
spec:
  bootstrapServers:
    - host.minikube.internal:39092

  topic: hc.scoring

  contentMode: binary  # Publish clean JSON (not CloudEvents envelope)
