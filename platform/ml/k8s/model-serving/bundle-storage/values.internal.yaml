fullnameOverride: serving-minio

auth:
  rootUser: <set-via-MINIO_ACCESS_KEY> # Standardized across all MinIO instances
  rootPassword: "<set-via-MINIO_SECRET_KEY>" # Standardized across all MinIO instances
  usePasswordFiles: false
  useSecret: true
  forceNewKeys: true

# Create bentoml-bundles and feast-registry buckets on init
defaultBuckets: "bentoml-bundles,feast-registry"

service:
  type: ClusterIP  # Internal only - serving pods access directly
  ports:
    api: 9000

console:
  enabled: true  # Useful for debugging
  image:
    registry: docker.io
    repository: bitnamilegacy/minio-object-browser
    tag: latest

networkPolicy:
  enabled: true
  allowExternal: false  # Only K8s pods need access
  addExternalClientAccess: false

persistence:
  enabled: true
  size: 20Gi  # BentoML bundles can be large (models + dependencies)
  storageClass: "standard"
  mountPath: /bitnami/minio/data

image:
  debug: true

defaultInitContainers:
  volumePermissions:
    enabled: false
