apiVersion: v1
kind: Service
metadata:
  name: mlflow-watcher
  labels:
    app: mlflow-watcher
spec:
  type: ClusterIP
  selector:
    app: mlflow-watcher
  ports:
    - name: http
      port: 80
      targetPort: http

